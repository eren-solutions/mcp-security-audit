[project]
name = "mcp-security-audit"
version = "1.0.0"
description = "MCP server for AI-powered code security auditing"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
dependencies = ["mcp>=1.20.0"]

[project.scripts]
mcp-security-audit = "server:main"
